<div class="bs-docs-section">
  <div class="row">
    <div class="col-lg-12">
      <div class="page-header">
        <h1>Register</h1>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-lg-6">
      <div class="bs-component">
        <form action="/account/register" method="post" >
          <fieldset>
            <div class="form-group">
              <% if account.email_verificated? %>
                <%= render :'account/register_confirm', locals: {account: account} %>
              <% else %>
              <label for="UserName">User Name</label>
              <input tpye='username' name="username" class="form-control" id="UserName"
                     aria-describedby="userNameHelp" placeholder="Enter User Name">
              <small id="userNameHelp" class="form-text text-muted">
                User name will be used for login.
              </small>

              <label for="Email">Email</label>
              <input type='email' name="email" class="form-control" id="Email"
                     aria-describedby="emailHelp" placeholder="Enter email">
              <small id="emailHelp" class="form-text text-muted">
                Your email address will not show on your public profile page.
              </small>
              <% end %>
            </div>
            <div class='form-group'>
              <input type="submit" class='btn btn-primary' value='submit'> 
            </div>
          </fieldset>
        </form>
      </div>
    </div>
  </div>
</div>
